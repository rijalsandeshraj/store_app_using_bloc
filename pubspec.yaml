name: store_app_using_bloc
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  animations: ^2.0.7
  hydrated_bloc: ^9.0.0
  http: ^0.13.5
  equatable: ^2.0.5
  path_provider: ^2.0.11
  flutter_staggered_grid_view: ^0.6.2
  lottie: ^2.2.0
  flutter_bloc: ^8.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/animations/
